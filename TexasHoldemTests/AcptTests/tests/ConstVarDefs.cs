namespace TexasHoldemTests.AcptTests.tests
{
    //a helper class to hold acpt test's vars to keep them equal acros all classes
    internal class ConstVarDefs
    {
        public const int UserId1 = 0; //saved user ID. this user should allready be in the system

        public const int RoomId = 0; //saved room ID. this room should *NOT* be in the system befour tests
    }
}
